void main() {
  var resultado = 3 + 4;
  var multiplier = 5;
  var operation = resultado * (multiplier + 5) / 5;
  var resto = 10 % 2;
  print(resto);

  // Adição +
  // Subtração -
  // Multiplicação *
  // Divisão /
  // Módulo %
}
